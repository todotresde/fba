int tamFont = 0;
int posX;
int posY;
int alpha = 0;

void setup(){
  size(400,400);
  posX = -width/2;
  posX = -height/2;
}

void draw(){
  background(200);
  textSize(20);
  fill(255, 0 ,0);
  text("El día de hoy voy a dar una clase para la comisión 4 y la comisón 5", 30, 100, 300, 400);
}
